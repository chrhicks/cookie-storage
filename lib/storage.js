(function (scope) {
  "use strict";
  scope.Storage = scope.Storage || (function() {
    var insertUpdate, retrieve;

    insertUpdate = function (k, v) {
      
    };

    retrieve = function(k) {
      
    };

    return {
      get: retrieve,
      put: insertUpdate  
    };
  }());  
}(this));